# Domain docs

This is a single-context repository.

Before changing domain behaviour, read `CONTEXT.md` and any relevant decisions under `docs/adr/`. Use the glossary's canonical terms in code, tests, specifications, and reports. Surface any conflict with an existing ADR instead of silently overriding it.
